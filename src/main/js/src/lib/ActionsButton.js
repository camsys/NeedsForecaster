import React from "react";

export const ActionsButton = ({actions}) => {
    return (
        <>
            <button className={"actions-button primary-button"} name={"actions"}>Actions</button>
        </>
    );
}